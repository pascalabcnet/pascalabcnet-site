---
title: Кортежи
description: Объединение нескольких значений, распаковка и возврат результатов.
slug: tuples
order: 50
group: Типы данных
---

Кортеж хранит несколько связанных значений, которые могут иметь разные типы.

## Создание кортежа

Например, кортеж состоит из имени и возраста персоны:

```pascalabc
begin
  var person := ('Анна', 20);
  Println(person.Item1, person.Item2);
end.
```

## Распаковка

Кортеж можно распаковать в переменные

```pascalabc
begin
  var point := (12, 7);
  var (x,y) := point;
  Println($'x = {x}, y = {y}');
end.
```

## Обмен значений переменных

Кортежное присваивание позволяет поменять значения двух переменных без дополнительной переменной.

```pascalabc
begin
  var a := 5;
  var b := 12;

  (a,b) := (b,a);

  Println(a,b);
end.
```

**Результат:**

```text
12 5
```

## Циклический сдвиг переменных

С помощью кортежного присваивания можно одновременно циклически переставить значения нескольких переменных.

```pascalabc
begin
  var a := 1;
  var b := 2;
  var c := 3;

  (a,b,c) := (b,c,a);

  Println(a,b,c);
end.
```

**Результат:**

```text
2 3 1
```

## Алгоритм Евклида

Кортежное присваивание позволяет компактно записать алгоритм Евклида нахождения наибольшего общего делителя двух чисел.

```pascalabc
begin
  var (a,b) := ReadInteger2;

  while b <> 0 do
    (a,b) := (b,a mod b);

  Println(a);
end.
```

Если введены числа:

```text
48 18
```

**Результат:**

```text
6
```

## Явный тип кортежа

Тип кортежа можно указать явно, перечислив типы его элементов.

```pascalabc
begin
  var t: (string,integer) := ('Иванов',18);

  Println(t.Item1,t.Item2);
end.
```

**Результат:**

```text
Иванов 18
```

## Сравнение кортежей

В PascalABC.NET определено лексикографическое сравнение кортежей

```pascalabc
begin
  var p1 := (2,5);
  var p2 := (3,1);

  Println(p1 < p2);
end.
```

## Функция, возвращающая кортеж

Можно определить функцию, возвращающую кортеж. Ее результат следует распаковать в переменные:

```pascalabc
function MinMax(a,b: integer) := if a < b then (a,b) else (b,a);

begin
  var (a,b) := ReadInteger2;
  var (minimum,maximum) := MinMax(a,b);
  Println(minimum,maximum);
end.
```

